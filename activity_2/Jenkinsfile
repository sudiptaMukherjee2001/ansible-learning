pipeline {
    agent any

    stages {
        stage("Run Ansible Playbook") {
            steps {
                withCredentials([
                    sshUserPrivateKey(
                        credentialsId: 'd2bfcac2-1f06-4f5a-a84f-f8cadab827a0',
                        keyFileVariable: 'private_key'
                    )
                ]) {
                    sh '''
                    mkdir -p ~/.ssh
                    chmod 700 ~/.ssh

                    
                    

                    ssh-keyscan -H 34.227.53.15 >> ~/.ssh/known_hosts
                    ssh-keyscan -H 18.212.86.97 >> ~/.ssh/known_hosts

                    chmod 600 ~/.ssh/known_hosts
                    '''

                    dir('activity_2') {
                        sh '''
                        ansible-playbook -i inventory deploy.yml --private-key $private_key -u ubuntu
                        '''
                    }
                }
            }
        }
    }
}
